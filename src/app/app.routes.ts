import { Routes } from '@angular/router';
import { HomeComponent } from './core/components/app.component'; // Import your home component or any other component you want to use in routing.

export const routes: Routes = [
  { path: '', component: HomeComponent },
  // Define other routes here
];
